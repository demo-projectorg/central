#!/usr/bin/env groovy
properties([
    [$class: 'GithubProjectProperty',
    displayName: '',
    projectUrlStr: 'https://github.com/harika-atmala/central.git/'],
    pipelineTriggers([githubPush()])])
properties([[$class: 'GithubProjectProperty', displayName: '', projectUrlStr: 'https://github.com/harika-atmala/central/'], pipelineTriggers([upstream('jenkins-multibranch-pipeline/branch2')])])



pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                
				echo 'Building stage'
				echo 'from branch3'
            }
        }
        stage('Test'){
            steps {
                echo 'Testing stage'
            }
        }
        stage('Deploy') {
            steps {
                echo 'deploying stage'
            }
        }
    }
}
