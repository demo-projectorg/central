#!/usr/bin/env groovy
properties([
    [$class: 'GithubProjectProperty',
    displayName: '',
    projectUrlStr: 'https://github.com/harika-atmala/central.git/'],
    pipelineTriggers([githubPush()])])

properties([pipelineTriggers([upstream('branch1')])])

pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                
				echo 'Building stage'
				echo 'From branch2'
            }
        }
        stage('Test'){
            steps {
                echo 'Testing stage'
            }
        }
        stage('Deploy') {
            steps {
                echo 'deploying stage'
            }
        }
    }
}
