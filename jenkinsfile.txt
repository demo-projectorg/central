#!/usr/bin/env groovy
properties([
    [$class: 'GithubProjectProperty',
    displayName: '',
    projectUrlStr: 'https://github.com/harika-atmala/central.git/'],
    pipelineTriggers([githubPush()])])


//properties([pipelineTriggers([upstream('branch3')])])


            
pipeline {
    agent any 
    
parameters([string(defaultValue: '', description: '', name: 'default'), choice(choices: [''], description: '', name: ''), booleanParam(defaultValue: false, description: '', name: '')]), pipelineTriggers([upstream('jenkins-multibranch-pipeline/master')])])

    stages {
        stage('Buil') { 
            steps { 
                sh 'pwd' 
                echo ' new update...'
            }
        }
        stage('Test'){
            steps {
                sh 'java -version'
                
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls'
                sh 'pwd'
            }
        }
    }
}
