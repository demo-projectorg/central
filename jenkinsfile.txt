#!/usr/bin/env groovy
properties([
    [$class: 'GithubProjectProperty',
    displayName: '',
    projectUrlStr: 'https://github.com/harika-atmala/central.git/'],
    pipelineTriggers([githubPush()])])

properties([[$class: 'GithubProjectProperty', 
             displayName: '', 
             projectUrlStr: 'https://github.com/harika-atmala/central.git/'], 
            pipelineTriggers([upstream(threshold: 'FAILURE', upstreamProjects: 'branch3')])])
pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                sh 'pwd' 
                echo ' new update'
            }
        }
        stage('Test'){
            steps {
                sh 'java -version'
                
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls'
                sh 'pwd'
            }
        }
    }
}
